#!/usr/bin/env bash
# Example status-line command. Harnesses that render a status line call this;
# harness-sync symlinks it into each one. Print a short, single-line status.
printf 'harness-sync example'
