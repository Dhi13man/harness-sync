# Example config repo (AGENTS.md)

Codex and other harnesses read `AGENTS.md` as their guidance file. `harness-sync`
keeps `CLAUDE.md` and `AGENTS.md` side by side so every harness gets the same
guidance from one source of truth. Edit both (or keep one a copy of the other).
